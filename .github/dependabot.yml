version: 2

updates:
  # Composer dependencies — guardrailed for a *library*, where wide version
  # ranges are intentional and the toolchain is deliberately pinned.
  - package-ecosystem: composer
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - composer
    commit-message:
      prefix: chore
      prefix-development: chore
    # One rollup PR for the dev toolchain instead of one PR per package.
    groups:
      dev-dependencies:
        dependency-type: development
    ignore:
      # We support the whole ^5.0 range on purpose; don't narrow the floor.
      - dependency-name: "craftcms/cms"
      # phpstan/phpstan is capped at ^1.x by craftcms/phpstan (dev-main);
      # a major bump would break the quality gate. Allow patch/minor only.
      - dependency-name: "phpstan/phpstan"
        update-types:
          - version-update:semver-major

  # The wizard's build/test toolchain. Grouped for the same reason as the PHP
  # dev dependencies: one rollup PR beats one per package.
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 3
    labels:
      - dependencies
      - javascript
    commit-message:
      prefix: chore
      prefix-development: chore
    groups:
      dev-dependencies:
        dependency-type: development

  # Keeps the actions the CI workflow pins from going stale.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    labels:
      - dependencies
      - ci
    commit-message:
      prefix: chore

