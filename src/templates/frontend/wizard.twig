{# ==========================================================================
   Slots — booking wizard dispatcher.

   Renders the framework-free vanilla wizard.

   The include path and all documented config variables are unchanged, so
   existing `{% include 'slots/frontend/wizard' %}` usage keeps working.
   ========================================================================== #}
{% include 'slots/frontend/wizard-vanilla' %}
