{#
/**
 * Upcoming Bookings
 * 
 * Shows all upcoming bookings for the user.
 */
#}

{% extends "slots/frontend/account/_layout" %}

{% set currentPage = 'upcoming' %}

{% block pageTitle %}{{ 'account.upcomingBookings'|t('slots') }}{% endblock %}

{% block accountContent %}
    <div class="slots-section section-plain">
        {% if bookings|length %}
            {% for booking in bookings %}
                {% include 'slots/frontend/account/_booking-card' with { booking: booking } %}
            {% endfor %}
        {% else %}
            {% include 'slots/frontend/account/_empty-state' with { message: 'account.noUpcomingBookings'|t('slots') } %}
        {% endif %}
    </div>
{% endblock %}
